import * as React from 'react';
import {Input} from 'react-figma-plugin-ds';

export default function ColorInput() {
    return <Input className="colorInput" type="color" name="color" id="color" />;
}
